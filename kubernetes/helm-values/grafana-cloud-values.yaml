prometheus:
  prometheusSpec:
    remoteWrite:
      - url: "REPLACE_WITH_YOUR_GRAFANA_CLOUD_REMOTE_WRITE_URL"
        basicAuth:
          username:
            name: grafana-cloud-credentials
            key: username
          password:
            name: grafana-cloud-credentials
            key: password
